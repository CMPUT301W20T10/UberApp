<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="backgroundColor">@color/white</item>
        <item name="textColor">@color/black</item>
        <item name="textHintColor">@color/colorPrimary</item>
        <item name="editTextBackgroundColor">@color/colorBackground</item>
        <item name="buttonColor">@color/colorPrimaryDark</item>
        <item name="buttonText">@color/white</item>
        <item name="themePicture">@mipmap/darkmode</item>
        <item name="listViewDivider">@color/colorPrimaryDark</item>

    </style>

    <style name="DarkTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="backgroundColor">@color/colorPrimaryDark</item>
        <item name="textColor">@color/white</item>
        <item name="textHintColor">#75FFFFFF</item>
        <item name="editTextBackgroundColor">@color/colorPrimary</item>
        <item name="buttonColor">@color/colorBackground</item>
        <item name="buttonText">@color/colorPrimaryDark</item>
        <item name="themePicture">@mipmap/lightmode</item>
        <item name="listViewDivider">@color/colorBackground</item>

    </style>

    <!-- Alert Dialog Dark -->
    <style name="ThemeOverlay_MaterialComponents_MaterialAlertDialog_Background" parent="@style/ThemeOverlay.MaterialComponents.MaterialAlertDialog">
        <!-- Background Color-->
        <item name="android:background">@color/colorPrimaryDark</item>
        <!-- Text Color for title and message -->
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

</resources>
